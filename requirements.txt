# Sterling Vance Bank — Enterprise AI Agent Platform Dependencies
# Covers MCP Server, Long-Term Memory, RAG Retrieval, and Planning & Task Decomposition

# Model Context Protocol (MCP) SDK
mcp>=1.2.0

# LLM Providers & API Clients
openai>=1.0.0
python-dotenv>=1.0.0
httpx>=0.25.0

# Production Streamable HTTP Transport & ASGI Web Server
starlette>=0.27.0
uvicorn>=0.22.0
anyio>=4.0.0

# JSON Schema Validation & Data Utilities
jsonschema>=4.18.0
pydantic>=2.0.0
typing-extensions>=4.8.0

# Memory & Context Evaluation Subsystems
tiktoken>=0.7.0               # Token counting and context window estimation

# RAG & Retrieval Subsystems
chromadb>=0.6.0               # Vector store with HNSW index and metadata filtering
rank-bm25>=0.2.2              # BM25 keyword scoring for hybrid search
sentence-transformers>=2.0.0  # Local semantic embeddings (all-MiniLM-L6-v2)
networkx>=3.0                 # Policy knowledge graph for Graph RAG

# Planning & Benchmarking Utilities
tabulate>=0.9.0               # Comparison table formatting
pytest>=7.0.0                 # Submodule and test discovery runner
